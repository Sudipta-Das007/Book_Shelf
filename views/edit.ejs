<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRUD</title>
    <link rel="stylesheet" href="/css/semantic.min.css">
    <link rel="stylesheet" href="/css/style.css">
	<style>
        
        h1 {
            text-align: center;
            font-size: 50px !important;
            font-style: italic;
        }
        input[type="submit"] {
            font-size: 15px !important;
        }
		label[for="book"] {
			font-size: 15px !important;
        }    
            label[for="author"] {
			font-size: 15px !important;
        }    
            label[for="isbn"] {
			font-size: 15px !important;
		}
        .ui.button {
            border-radius: 10px;
        }
        .ui.button.yellow.fluid {
            background-color: #EFE162;
            color: rgb(0, 0, 0);
        }
        .ui.yellow.button:hover {
            background-color: #21BA45;
            color: #FFFFFF;
        }
    </style>
</head>

<body>
    <div class="wrapper">

        <br>
        <br>
        <br>
        <div class="ui container">
            <h1>Update Book Information</h1>
            <form id="form" action="/edit/<%= club._id%>" class="ui form" method="POST">
                <div class="ui three fields">
                    <div class="field">
                        <label for="book" class="ui header black">Update the name of Book</label>
                        <input type="text" name="book" placeholder="Enter the name of book" value="<%= club.book %>" id="book">
                    </div>
                    <div class="field">
                        <label for="author" class="ui header black">Update the name of Author</label>
                        <input type="text" name="author" placeholder="Enter the name of the author" value="<%= club.author %>"  id="author">
                    </div>
                    <div class="field">
                        <label for="isbn" class="ui header black">Update the ISBN</label>
                        <input type="text" name="isbn" placeholder="Enter the isbn" value="<%= club.isbn %>"  id="isbn">
                    </div>
                </div>
                <div class="ui fluid field">
                    <input type="submit" value="Submit" class="ui button yellow fluid">
                </div>
            </form>

            <br>
            <br>
        </div>
    </div>

    <script src="/js/semantic.min.js"></script>
</body>

</html>